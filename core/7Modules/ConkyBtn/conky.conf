-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html

conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=10',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
    own_window_transparent = false,
    own_window_colour = '2b4e5e',
}

-- ${color grey}Frequency (in MHz):$color $freq
-- $cpu% ${cpubar 4}

conky.text = [[
 ${color grey}Info:$color $sysname $nodename $kernel $machine}
$hr
 ${color grey}Uptime:$color $uptime
 ${color grey}Frequency (GHz):$color $freq_g
 ${color grey}RAM Usage:$color $mem tot:$memmax - $memperc% ${membar 4}
 ${color grey}Swap Usage:$color $swap tot:$swapmax - $swapperc% ${swapbar 4}
 ${color grey}CPU Usage:$color $cpu% ${color grey}  Temp:$color ${hwmon temp 1}°C
 ${color grey}Processes:$color $processes  ${color grey}Running:$color $running_processes
 ${color2}${color grey}Battery:  \
${if_existing /sys/class/power_supply/BAT0/status}${voffset 1}${color6}${battery_bar 6,80 BAT0}  ${voffset -1}${color3}${battery_percent BAT0}%${endif}\
${if_existing /sys/class/power_supply/BAT1/status}${voffset 1}${color6}${battery_bar 6,80 BAT1}  ${voffset -1}${color3}${battery_percent BAT1}%${endif}
$hr
 ${color grey}File systems:
 $color ${execp cat ~/.fvwm/core/7Modules/ConkyBtn/hdd-storage.conf | sed 's/  Local Storage/HDD/'}
 $color ROOT:  $color${fs_size /} ${color grey}used $color${fs_used /} ${color grey}free $color${fs_free /}
   /     ${fs_bar 6 /}
 $color HOME:  ${fs_size /home} ${color grey}used $color${fs_used /home} ${color grey}free $color${fs_free /home} 
   /home ${fs_bar 6 /home}
 ${color grey}Networking:
   Up:$color ${upspeed} ${color grey} - Down:$color ${downspeed}
$hr
]]
